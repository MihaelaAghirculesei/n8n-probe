# n8n's packages pull a large, loosely-pinned peer graph; do not fail the
# install over peer mismatches, but keep them visible.
strict-peer-dependencies=false
auto-install-peers=true

# Deterministic installs in CI.
resolution-mode=highest
